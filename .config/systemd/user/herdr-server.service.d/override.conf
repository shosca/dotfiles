# Image paste needs the desktop session's display env; systemd user services
# don't always inherit it depending on startup order. Wayland only, no X11.
[Service]
Environment=WAYLAND_DISPLAY=wayland-0
